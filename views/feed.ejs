<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Feed</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body class="bg-zinc-900 text-white min-h-screen flex flex-col items-center">

  
  <div class="container max-w-4xl mx-auto flex items-center justify-between bg-zinc-800 p-6 rounded-lg shadow-lg mt-8">
    <div class="profile flex items-center gap-6">
      <img class="h-24 w-24 rounded-full object-cover border-4 border-pink-600" src="<%= user.profileImage %>" alt="Profile Image">
      <h1 class="text-xl font-semibold text-yellow-100">Hey... <span class="text-2xl text-yellow-300"><%= user.username %></span></h1>
    </div>
    <div class="create-post">
      <a href="/post/create"> Create New Post</a>
    </div>
    <div class="routes flex gap-4">
      <a href="/user/login" class="text-blue-400 hover:underline">Switch Account</a>
      <a href="/user/logout" class="text-red-400 hover:underline">Logout</a>
    </div>
  </div>





  

  
  <div class="postContainer max-w-4xl w-full mt-6 p-6 bg-zinc-800 rounded-lg shadow-lg">
    <h2 class="text-2xl font-bold border-b border-zinc-600 pb-2 mb-4">Posts</h2>
    <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
      <% user.posts.forEach((i) => { %>
        <div class="post bg-zinc-700 p-4 rounded-lg shadow-md">
          <p class="text-lg font-medium mb-2 text-yellow-200"><%= i.caption %></p>
          <img class="w-full h-60 object-cover rounded-lg border border-zinc-600" src="<%= i.media %>" alt="Post Image">
        </div>
      <% }) %>
    </div>
  </div>

</body>

</html>